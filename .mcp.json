{
  "mcpServers": {
    "deriva-ml": {
      "command": "uv",
      "args": ["run", "deriva-ml-mcp"],
      "cwd": "/Users/carl/Repos/Projects/deriva-ml-mcp"
    },
    "github": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "-e", "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
